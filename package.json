{
  "name": "dra-vitoria-gomes-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Site institucional + captação — Dra. Vitória Gomes (Psiquiatria Funcional Integrativa · Telemedicina)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "build:fast": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "gen-images": "node scripts/gen-images.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=dra-vitoria-gomes",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=dra-vitoria-gomes --branch=preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.2.1",
    "@astrojs/tailwind": "^5.1.4",
    "astro": "^5.1.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2"
  }
}
